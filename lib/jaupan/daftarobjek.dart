import 'package:flutter/material.dart';

class DaftarObjekPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Daftar Objek")),
      body: Center(
          child: Text("Halaman Daftar Objek", style: TextStyle(fontSize: 18))),
    );
  }
}
