import 'package:flutter/material.dart';

class DaftarTembakPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Daftar Tembak")),
      body: Center(
          child: Text("Halaman Daftar Tembak", style: TextStyle(fontSize: 18))),
    );
  }
}
